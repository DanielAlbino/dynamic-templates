import React from 'react';
import ReactDOM from 'react-dom';
import Main from './pages/main';

const App: React.FC = () => {return <Main />;};

ReactDOM.render(<App />, document.getElementById('root'));